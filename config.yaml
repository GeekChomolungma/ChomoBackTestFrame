
# === Backtest Config (edit me) ===
# Secrets: put MONGODB_URI in `.env` (see .env.example).

# Optional CSV fallback (if set, use CSV instead of Mongo):
# csv_path: "/path/to/klines.csv"

mongo_db: "market_info"
mongo_coll: "BTCUSDT_4h_Binance"

symbol: "BTCUSDT"
interval: "4h"

# ISO8601 or epoch ms
start: "2025-03-01T00:00:00Z"
end:   "2025-09-01T00:00:00Z"

ckpt_path: "model/checkpoints/ohlcv/20250830-122134_best_forBTC.pt"
device: null

fee_bps: 0.0
slippage_bps: 0.0
initial_capital: 10000.0
threshold: 0.005
